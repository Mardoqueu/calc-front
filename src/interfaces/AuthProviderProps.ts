import { ReactNode } from "react";

/**
 * AuthProviderProps is an interface that specifies the props for the AuthProvider component.
 *
 * Properties:
 * - `children`: The ReactNode elements that are to be rendered within the AuthProvider component.
 */
export interface AuthProviderProps {
  children: ReactNode;
}
