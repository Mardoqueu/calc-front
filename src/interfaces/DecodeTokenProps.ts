/**
 * The DecodeTokenProps interface represents the properties decoded from a token.
 *
 * Property:
 * - exp (number): The expiration time of the token, represented as a UNIX timestamp.
 */
export interface DecodeTokenProps {
  exp: number;
}